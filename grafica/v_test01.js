function setup(){
	createCanvas(windowWidth, windowHeight);

}
function draw(){
	ellipse(random(width), random(height), 10, 10)
}

function reset(){
	background(255);
}
function mousePressed(){
	background(255);
}